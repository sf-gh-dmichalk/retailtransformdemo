name: 'retail_transform'
version: '1.0.0'

profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]

models:
  retail_transform:
    staging:
      +materialized: view
      +schema: BRONZE
    silver:
      +materialized: incremental
      +schema: SILVER
    gold:
      +materialized: table
      +schema: GOLD
